{
  "name": "alertrix-backend",
  "version": "1.0.0",
  "private": true,
  "description": "AWS Lambda + API backend for Alertrix",
  "main": "src/handlers/getReadings.js",
  "type": "commonjs",
  "scripts": {
    "lint": "echo \"add linter if needed\"",
    "test": "echo \"add tests if needed\""
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.862.0",
    "@aws-sdk/client-sesv2": "^3.862.0",
    "@aws-sdk/client-sns": "^3.862.0",
    "@aws-sdk/lib-dynamodb": "^3.862.0",
    "firebase-admin": "^13.6.0"
  }
}
